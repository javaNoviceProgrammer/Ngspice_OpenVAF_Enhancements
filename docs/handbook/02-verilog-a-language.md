# 2 · Verilog-A language support

This is the feature matrix: what the compiler accepts, what the compiled
model actually *does* at simulation time, and where each claim is proven.
The scope of the project is **Verilog-A** — the analog subset defined in
Annex C of the Verilog-AMS LRM — not full mixed-signal AMS (see
[chapter 4](04-limitations-and-gotchas.md#41-scope-verilog-a-not-full-ams)
for exactly where that line runs).

Every table row links the enhancement that built or audited the feature
(`E-n` → the detailed write-up in `enhancements_doc/`) and, in the notes,
the `examples/` folder whose verify script pins it. A recurring lesson of
this project is that *compiling proves nothing* — several features compiled
for years while silently doing the wrong thing at runtime — so a row only
says "works" if a committed script checks the simulated numbers.

## 2.1 Modules, hierarchy, and elaboration

Hierarchy is resolved by a compile-time **elaboration pass**: instantiated
modules are recursively inlined (alpha-renamed per instance, ports bound to
the caller's nets, parameters bound to overrides) into a single flat module
before the rest of the compiler runs. Everything in this section is
therefore resolved at **compile time**.

| Feature | Notes | Since |
|---|---|---|
| Module instantiation | Named `.p(net)` and positional port connections, open ports, named `#(.r(1e3))` and positional `#(1e3)` parameter overrides, instance arrays `res rarr[0:3](…)`, arbitrary nesting, across `` `include `` boundaries; cyclic instantiation is a clean error. Mixing positional and named forms in one `#(...)` list is an error (LRM Syntax 6-2; the positional half used to be silently dropped), and a connection that does not match a multi-bit port's width — e.g. a scalar net on a 2-bit port — is an error citing LRM 6.5.7.1 instead of a silent per-bit broadcast. Since E-637 every bus actual connects **positionally**: a whole bus in its declared order, a part-select in the order written, a `{...}` concatenation leftmost-first, each onto the port's declared msb-to-lsb order — so `.a(p)` and `.a({p})` agree when the declarations run opposite ways, and `.a(p[3:0])` of a `[0:3]` bus reverses like `{p[3],p[2],p[1],p[0]}` (it used to mean `p[0:3]`). `examples/instantiation_examples/`, `examples/busorder_examples/` | [E-5](../../enhancements_doc/Enhancement-5.md), [E-637](../../enhancements_doc/Enhancement-637.md) |
| `generate for` / `genvar` | Structural loops generating nets, instances, variables, parameters; nested loops (inner bounds may use outer genvars); genvars usable in any expression, e.g. `#(.r(1e3*(i+1)))`. Since E-564 what a block declares is reachable by its **hierarchical name** (LRM 6.7): `V(g1[0].z)` for an iteration, `V(g1[0].genblk1.w)` for a block nested in it, `g1[1].r1.mid` for an instance inside it. Since E-638 an assignment to a genvar inside its own analog loop (`i = i + 1;`), or a nested loop reusing the genvar, is refused with the statement quoted — it used to surface as a parser error at `0 = 0 + 1` in a generated copy. `examples/generate_examples/`, `examples/genhier_examples/`, `examples/genvarassign_examples/` | [E-8](../../enhancements_doc/Enhancement-8.md), [E-67](../../enhancements_doc/Enhancement-67.md), [E-564](../../enhancements_doc/Enhancement-564.md), [E-638](../../enhancements_doc/Enhancement-638.md) |
| `generate if` / `generate case` | else/else-if chains, multi-value case arms, default; conditions fold over genvars and literals; a branch or arm may be a **single item** without `begin`/`end` (LRM 6.6.2). Since E-564 a named block is reachable as `V(blk.x)`, a case arm as `V(two.q)`, and an unlabelled block by its **implicit `genblk<n>` name** (LRM 6.6.3: constructs numbered in textual order, leading zeroes added while the name is declared). A condition on a module *parameter* is an intentional error — parameters bind at simulation time and cannot shape structure (see [§4.3](04-limitations-and-gotchas.md#43-compile-time-vs-simulation-time)). | [E-67](../../enhancements_doc/Enhancement-67.md), [E-564](../../enhancements_doc/Enhancement-564.md) |
| Hierarchical names | Instance-path references `u1.u2.node`, `$root.top.net`, from expressions and probes. A **contribution** targeting a hierarchical net (`V(p, c1.mid) <+ 0.5;`) creates its own branch in the contributing module per LRM 5.6.8.1 — distinct from any branch the child has between the same nodes — so the child's contributions and probes are unaffected; references to a child's *named* branch merge per 5.6.8.2. `examples/hiername_examples/` | [E-49](../../enhancements_doc/Enhancement-49.md) |
| `defparam` | Compile-time hierarchical parameter override at any depth (`defparam u1.u2.r = 2e3;`), multi-assignment form, expression values; takes LRM-mandated precedence over instance `#()` overrides. Works inside generate blocks (targeting the per-iteration instance, genvars usable in the value) and at module scope beside generate constructs — both used to be silently dropped by the generate rewrite. `examples/defparam_examples/` | [E-58](../../enhancements_doc/Enhancement-58.md) |
| `paramset` | Verilog-AMS paramset blocks per LRM 6.4, including `.$mfactor`/position hidden-system-parameter assignments inside the paramset. Since E-563: a paramset's parameters, aliases and variables **reuse the module's names** (`parameter real L = 3u; .L = L;`); a paramset may target **another paramset** and assign the parent's own parameters (a chain); an **`aliasparam`** of the paramset names its parameter on the card and in an instance override; **variables and statements** (6.4.1) compute **output variables** (6.4.3) from the module's through `.name` — a same-named one replaces the module's; **hierarchical references to another module's `localparam`s** (the "constant module" idiom, `.RSH = fab.rsh_poly * fab.bias;`); and a paramset **instantiated inside a module** carries all of this (its bindings used to be lost there). Since E-565 paramsets may be **overloaded** (6.4.2): the family compiles to `nch`, `nch__2`, …, an instance inside a module is resolved at elaboration and a `.model nch …` card by ngspice as it is materialised, both by the clause's rules and tie-breaks, an ambiguity or a card no member accepts being an error; since E-643 the `.model` route judges and counts **only the paramset's own parameters** (a pass-through module parameter neither disqualifies nor tips a tie), reads `{…}` value sets, and a tie names the ranges that would break it; since E-644 **a paramset's own parameters are instance parameters** (the card gives the instances' defaults, `(* type="model" *)` keeps one on the card) and the member is selected **per instance** — `n1 a b rsil l=0.5u w=0.5u mm_ok=1` — the first instance binding the card and one needing another member getting a clone card `<card>.<member>`. Since E-645 a paramset assigns an **array parameter whole**, `.c = '{10.0, 20.0, 30.0};`, each element taking the leaf at its own position (row-major for N-D; the paramset's own parameters may appear in the leaves), a literal of the wrong length or a scalar refused with the counts and an element outside the declared range refused by its element name. Still refused: a random draw in an override (E-545). `examples/paramarrayarg_examples/`, `examples/paramset_examples/`, `examples/paramsethsp_examples/`, `examples/paramsetlrm_examples/`, `examples/paramsetoverload_examples/`, `examples/cardbound_examples/`, `examples/paramsetinst_examples/` | [E-21](../../enhancements_doc/Enhancement-21.md), [E-44](../../enhancements_doc/Enhancement-44.md), [E-563](../../enhancements_doc/Enhancement-563.md), [E-565](../../enhancements_doc/Enhancement-565.md), [E-645](../../enhancements_doc/Enhancement-645.md) |
| Implicit nets | An undeclared identifier in an instance connection becomes an implicit scalar net, discipline derived from the connected port. `examples/implicitnet_examples/` | [E-41](../../enhancements_doc/Enhancement-41.md) |
| Net concatenation in ports | `u1({a, c})` onto a vectored port, expanded bit-by-bit (leftmost = msb), positionally or named, nested through instance levels; width mismatch is a hard error. | [E-59](../../enhancements_doc/Enhancement-59.md) |
| Multiple `analog` blocks | Several `analog` / `analog initial` blocks per module execute as-if-concatenated in source order (LRM 6.2), including across instance flattening. `examples/multianalog_examples/` | [E-60](../../enhancements_doc/Enhancement-60.md) |

## 2.2 Nets, disciplines, natures

| Feature | Notes | Since |
|---|---|---|
| Vectored (bus) nets and ports | `electrical [3:0] bus;` with bit-select access in branches and probes; buses slice element-wise onto instance arrays and bus ports. A signal-access index may be any constant expression over literals, genvars, and **parameters** (`V(in[width-2])`, LRM 5.5.2) — the index selects a node, so parameters it reads freeze structural like parameter-shaped widths. `examples/bus_examples/` | [E-3](../../enhancements_doc/Enhancement-3.md) |
| `ground` declarations | Both orderings (`electrical ground gnd;` and `ground electrical gnd;`). `examples/ground_examples/` | [E-9](../../enhancements_doc/Enhancement-9.md) |
| Derived natures | `nature X : Parent`, deriving from a discipline's nature (`: electrical.flow`), with full attribute inheritance. `examples/derivednature_examples/` | [E-39](../../enhancements_doc/Enhancement-39.md) |
| Net initializers (nodesets) | `electrical a = 5.0;` becomes an OSDI nodeset — an initial-guess hint for the solver, essential for bistable circuits. `examples/netinit_examples/` | [E-45](../../enhancements_doc/Enhancement-45.md) |
| Nature-attribute access | `net.potential.abstol`, `branch.potential.abstol` readable in expressions. | [E-45](../../enhancements_doc/Enhancement-45.md), [E-59](../../enhancements_doc/Enhancement-59.md) |
| Nature-attribute values | An attribute value is a constant expression, not only a signed literal (LRM A.1.6): `abstol = 1e-3*1e-3`, a `` `define `` in arithmetic, a discipline's `potential.abstol = 1e-3*1e-3` override. Two integers make an integer (`1/1000000` is 0 and is refused as such), any real makes the result real, and the folded value is the one ngspice's convergence test applies (E-539) and `net.potential.abstol` reads. A name or a function call is still not a constant here. `examples/natureexpr_examples/` | [E-649](../../enhancements_doc/Enhancement-649.md) |
| Domain binding | `domain continuous` accepted; `domain discrete` on a discipline with natures is a proper two-label error (LRM 3.6.2.2). `examples/domainbind_examples/` | [E-50](../../enhancements_doc/Enhancement-50.md) |
| Signal-flow / flow-only disciplines | Disciplines with only a flow (or only a potential) nature work, including probe-only usage (§2.7). `examples/signalflow_examples/` | [E-36](../../enhancements_doc/Enhancement-36.md) |
| Discipline compatibility (LRM 3.11.1) | The full rule set: a branch between a conservative net and a signal-flow net of the same potential nature is legal (`electrical`/`voltage`), natureless disciplines match their whole domain, domainless ones match everything; genuinely incompatible pairs (`electrical`/`rotational`) still refused. | — |
| NIST2018 physical constants | `constants.vams` is the VAMS-2023 Annex D.2 file: `` `define PHYSICAL_CONSTANTS_NIST2018 `` before the include selects the exact 2019-SI values; the default stays NIST1998 per the LRM. | — |

## 2.3 Data types, variables, and arrays

| Feature | Notes | Since |
|---|---|---|
| `real`, `integer`, `string` variables | Full support, including uninitialized strings defaulting to `""`. `examples/vartype_examples/` | [E-9](../../enhancements_doc/Enhancement-9.md) |
| Variable persistence | Module-level variables genuinely keep their value across evaluations (`accum = accum + 1.0;` accumulates) — for `real` *and* `integer` state. `examples/variable_persistence_examples/`, `examples/intstate_examples/` | [E-7](../../enhancements_doc/Enhancement-7.md), [E-32](../../enhancements_doc/Enhancement-32.md) |
| Declaration initializers | `real x = 2.0*p;`, including on (multi-dimensional) arrays; parameter-only initializers evaluate once at setup. `examples/varinit_examples/` | [E-43](../../enhancements_doc/Enhancement-43.md) |
| Arrays, 1-D to N-D | Both declaration orders (`real [0:3] x;` and LRM-style `real x[0:3];`), any number of dimensions, constant *and dynamic* indexing (`m[i][j]` with runtime `i`, `j`). A constant index outside the declared range is refused at compile time; a dynamic one is judged per dimension at run time and, since E-636, reported once per accepted point (`index [3] of `a`, declared [0:2], is out of range; the read returns a[0]` / `the assignment is dropped`) — the read is the first element and the write is dropped (E-489's no-out-of-bounds select). `examples/array_examples/`, `examples/mdarray_examples/`, `examples/arrayrange_examples/` | [E-14](../../enhancements_doc/Enhancement-14.md), [E-15](../../enhancements_doc/Enhancement-15.md), [E-18](../../enhancements_doc/Enhancement-18.md), [E-636](../../enhancements_doc/Enhancement-636.md) |
| Array aggregates | Whole-array assignment `c = '{v0, v1, v2};`, nested for N-D, array-to-array copy `c = d;`. | [E-14](../../enhancements_doc/Enhancement-14.md), [E-15](../../enhancements_doc/Enhancement-15.md) |
| Concatenation / replication | `{a, b, c}` and `{n{x}}` as real operators over arrays and strings (`'{…}` stays the typed aggregate); and, where a **scalar** is expected of it, a `{…}` of scalar integers is the LRM 4.1.13 **bit-level** concatenation — `{4'hA, 4'h5}` is 165, `{2{4'b1010}}` is 170, sized literals contribute their size and any other integer 32 bits, an unsized literal is refused and a result wider than 32 bits is warned about (F12 of the 2026-09-05 book audit). `examples/concat_examples/` | [E-34](../../enhancements_doc/Enhancement-34.md) |
| Arrays in `case` | Element-wise array `case` statements and array-literal case labels. `examples/arraycase_examples/` | [E-33](../../enhancements_doc/Enhancement-33.md) |

## 2.4 Parameters

| Feature | Notes | Since |
|---|---|---|
| Ranges and excludes | `from (0:inf)`, `exclude 0`, etc., enforced on user-given values. Per the CMC convention, a parameter's **default is exempt** from its own range — industry models use an out-of-range default as the "feature disabled" state. Since E-532 a **constant** default that violates its own constant range is no longer silent: the `param_default_out_of_range` lint (L027, warn by default) names it at compile time, and the deliberate must-give/feature-disabled idiom silences it per declaration with `(* openvaf_allow="param_default_out_of_range" *)`. All 26 bundled industry models compile with zero L027 hits. Since E-555 a default whose **bounds read another parameter** (`l = 1.2 from [lmin:inf)`) *is* judged at setup, given or not — the range can have moved since the default was declared, by `altermod`, a `.dc` sweep or an `osdimc` draw of `lmin`, and it was running silently below its bound; the constant-range exemption is untouched. Since E-635 an **integer** parameter's real bounds are compared as reals — `parameter integer k from (0.5:2.5]` admits 1 and 2 (the bounds used to be rounded to integers first, so 1 was refused and 3 accepted, and `from (1.5:2.5)` admitted nothing), `inf` is the real infinity, and a range no integer satisfies (`from (2:3)`) is refused at compile time. `examples/paramgiven_examples/`, `examples/intrange_examples/` Since E-650 a `from {…}` member of an **integer** parameter that no integer equals (`from {1, 2.5}`) draws the `dead_range_member` lint (L034, warn): the member is dead, and most likely a typo. | [E-56](../../enhancements_doc/Enhancement-56.md), [E-532](../../enhancements_doc/Enhancement-532.md), [E-555](../../enhancements_doc/Enhancement-555.md), [E-635](../../enhancements_doc/Enhancement-635.md), [E-650](../../enhancements_doc/Enhancement-650.md) |
| `localparam` | Non-overridable per the LRM; derived localparams (`localparam G = 1/R`) track their inputs. `examples/localparam_examples/` | [E-9](../../enhancements_doc/Enhancement-9.md) |
| `aliasparam` | Works, including `$param_given` queried through the alias. LRM 3.4.7's error rules are enforced: setting a parameter by its original name **and** an alias on one card/line is a netlist error, and referencing the alias in module equations is a compile error. | [E-59](../../enhancements_doc/Enhancement-59.md) |
| Array-valued parameters | `parameter real [0:3] w = '{…};` (any dimensionality) expands to one scalar OSDI parameter per element, each individually overridable from SPICE: `.model m dev(w[1][1]=0.9)`. The whole array can also be overridden at instantiation with an assignment pattern — `leaf #(.w('{…})) l1(a,b);` — checked against the declared element count (LRM 3.4.4/3.4.8). Compile time is linear in the element count since [E-713](../../enhancements_doc/Enhancement-713.md) — a 20 000-element `localparam` literal in 1 s (15 s before), 100 000 in 14 s (unfinished before); the descriptor's given-flag functions are a bitfield read for any parameter count. | [E-14](../../enhancements_doc/Enhancement-14.md), [E-15](../../enhancements_doc/Enhancement-15.md), [E-713](../../enhancements_doc/Enhancement-713.md) Since E-715 an element (`w[1]`) may be read in another parameter's default, a range bound, a `localparam`, another array's literal or a variable's initialiser, under the declaration-order rule. |
| String parameters | Including `case` dispatch on a string parameter. | [E-59](../../enhancements_doc/Enhancement-59.md) |
| Hidden system parameters | `$mfactor` (device multiplicity — automatically scales flows, noise, and Jacobians) and the position parameters, readable in the model and settable per instance (`n1 a b mod m=4`) — and per **child instance** inside the hierarchy: `leaf #(.$mfactor(4)) u1 (p, n);` applies the full LRM 6.3.6 multiplicity transform to the inlined child (reads compose with the netlist value, flow contributions ×m, flow probes read per-copy, noise power scales as the parallel combination), with `.$xposition(...)`-family overrides composing additively. | [E-44](../../enhancements_doc/Enhancement-44.md) |
| Instance-line parameters | `(* type="instance" *)` on a parameter exposes it on the instance line and to `alter @n1[p]` / `.dc @n1[p]` (see [§3.3](03-ngspice-workflows.md#33-parameter-access-alter-and-sweeps)). Since E-546 a parameter whose **default** reads an instance parameter (`parameter real l = 2*w;` with an instance `w`, transitively, `$param_given(w)` included) is itself resolved per instance — promoted to the instance line, named by the `instance_dependent_parameter` lint (L028, warn; silent for an untyped `localparam`), and still settable on the `.model` card as the card's default. A parameter whose **range** reads an instance parameter (`from (0:w]`, the shape BSIM6's `XGL` and HiSIM2's `LP` use) keeps its level and is judged per instance, with that instance's values; before E-546 both were resolved once per card, against the instance parameter's default — which is what BSIMCMG's `LSP = 0.2*(L+XL)`, a model parameter by declaration, was silently getting. `examples/instdep_examples/` | [E-62](../../enhancements_doc/Enhancement-62.md), [E-546](../../enhancements_doc/Enhancement-546.md) |
| Reserved parameter names | A module parameter named `m`, `temp`, `dtemp` or `dt` receives the netlist's `m=`/`temp=` and ngspice's own meaning is lost silently — the multiplier no longer applies, the instance temperature never reaches `$temperature`. Lint L029 `reserved_parameter_name` (warn, case-insensitive) says so; rename it, or for `m` apply the multiplier yourself. Since E-652 the whole exported name surface is judged the way ngspice looks it up: lint L035 `exported_name_collision` reports two names of one table that differ only by case (a parameter and a variable, two variables, an alias and a variable, two model parameters), naming which one comes first and wins, a variable named `m`/`temp`/`dtemp`/`dt` (ngspice's own wins) and one shadowing a synthesized terminal current (`i_<port>`, `i`); L036 `dollar_in_exported_name` reports a `$` in a name, which ngspice's expression parser reads as a shell variable (write-only, or not exported for a variable). Since [E-665](../../enhancements_doc/Enhancement-665.md) L036 names any character ngspice's parsers cannot read in an exported name — an escaped identifier's `+` or space as well as `$`; an array element's `[0]` and a dotted child path are read and stay silent. `examples/exportname_examples/` | [E-588](../../enhancements_doc/Enhancement-588.md), [E-652](../../enhancements_doc/Enhancement-652.md) |
| Contribution to an `input` port | `input p; … I(p,n) <+ …` drives a port the module declared it only reads. Lint **L031 `contribution_to_input_port`** (warn) is the warning LRM 5.6.1 says an implementation may issue — once per contribution statement whose branch has an input-only port at either end, flow or potential, named branch or not; probes (`V(p,n)`, `I(<p>)`) are not judged, `inout`/`output` ports are not inputs, and `(* openvaf_allow="contribution_to_input_port" *)`, `-A L031`, `-E L031` work as for every lint ([E-639](../../enhancements_doc/Enhancement-639.md)). |
| `$mfactor` in a flow contribution | `I(a,b) <+ V(a,b) / r * $mfactor` is the LRM's `badres`: the simulator multiplies every flow contribution by `$mfactor` itself (LRM 6.3.6), so the value is scaled twice. Lint **L037 `mfactor_double_scaling`** (warn) reports a flow contribution whose value depends on `$mfactor`, directly or through a variable assigned from it, at the read; an `if`/`?:` condition (the LRM's `parares`), a potential contribution, a display and an opvar stay silent; a noise contribution to a flow branch is reported. `(* openvaf_allow="mfactor_double_scaling" *)`, `-A L037`, `-E L037` work as for every lint ([E-686](../../enhancements_doc/Enhancement-686.md)). |
| Lossy integer constants | An integer literal wider than 32 bits is read as a real and saturates when stored into an `integer`; an `integer` parameter with a fractional or overflowing constant default is rounded or clipped. Lint **L030 `lossy_integer_constant`** (warn) reports both, once, with what the value became; `-A lossy_integer_constant` or `-A L030` silences it. Lint ids print as `warning[L030]` and are accepted by `-A`/`-W`/`-E`; `--lints` prints each id beside its name ([E-590](../../enhancements_doc/Enhancement-590.md)). The folder behind L030 and L027 divides two integers as integers, LRM 4.2: `7/2` is 3, not "the default 3.5", and `7/2*2 from [0:6]` is judged 6 ([E-664](../../enhancements_doc/Enhancement-664.md)). Since [E-675](../../enhancements_doc/Enhancement-675.md) an integer expression is evaluated the way the model computes it, in 32-bit two's-complement arithmetic: `2147483647 + 1` is "wraps to -2147483648", not "clipped" (only a real value is clipped, by its conversion), `2 ** 31` is folded, and the range check judges the stored value. Since [E-693](../../enhancements_doc/Enhancement-693.md) the integer `**` itself wraps: it was the float power through the saturating cast, so `(46341*k) ** 2` read 2147483647 where `(46341*k) * (46341*k)` is -2147479015; a constant exponent is the minimal multiply chain (`x ** 2` is `x * x`), a run-time one the unrolled 31-bit chain, Table 5-6 for a negative exponent unchanged, and L030 says "wraps to -2147483648" for `2 ** 31` like the product spelling. Since [E-708](../../enhancements_doc/Enhancement-708.md) a based literal wider than its size draws L030 too — `'hFFFFFFFFFF` is "40 significant bits, more than the 32 of an `integer` … it reads as -1", `8'hFFF` likewise against its declared size — where it was truncated in silence; a 100 000-digit literal is measured by digit count and abbreviated in the message. |
| `$param_given`, `$port_connected` | Both work; industry models use them for configuration guards. A parameter overridden from inside the hierarchy (instance `#(...)` value or `defparam`) reports *given* per LRM 6.3.5, even though flattening bakes the value in as the new default. `$port_connected` follows LRM 9.19 exactly — connected at the instance's own instantiation line whatever the enclosing level leaves open, a dangling net or ground counts as connected, an omitted trailing terminal or an open `.q()` reads 0, bits of a vector port by bit-select — pinned end-to-end on both solvers by `examples/portconnected_examples/`. | — |

## 2.5 Operators and literals

The operator surface was audited systematically — every operator against the
LRM, every precedence level against LRM Table 4-2 — with self-checking
modules whose failure modes are numerically visible.

| Feature | Notes | Since |
|---|---|---|
| Arithmetic, comparison, logical, bitwise operators | Audited; fixes included `~` (was arithmetic negate) and const-folded unsigned `>>`. `examples/operator_examples/` Since [E-706](../../enhancements_doc/Enhancement-706.md) `0.0/0.0` is a compile error ("the operands are both 0, so the quotient is undefined; the result would be NaN"), `1.0/0.0` folds to IEEE's infinity — legal on its own, refused by a consumer that needs a finite number (a delay, a step bound, a parameter default) — and `exp(1000.0)`, `cosh(1000.0)`, `pow(10.0, 400.0)` and `10.0 ** 400.0` are refused as exceeding the largest double; the folded NaN used to pass every constant-argument check. `examples/powguard_examples/` | [E-37](../../enhancements_doc/Enhancement-37.md), [E-706](../../enhancements_doc/Enhancement-706.md) |
| Precedence and associativity | Audited against LRM Table 4-2; `%` now binds like `*`/`/`, `2**3**2 = 64` (left-assoc), unary binds above `**`. `examples/precedence_examples/` | [E-38](../../enhancements_doc/Enhancement-38.md) |
| Arithmetic shifts `<<<` / `>>>` | A flagged extension: LRM 4.2.11 bars them from analog blocks, so each use draws a warning, but `>>>` is the only spelling of a sign-extending shift. A shift distance outside 0..=31 (any spelling) warns and yields the LRM-defined value (`1<<32` is 0 — the distance is unsigned). `examples/shift_examples/` | [E-6](../../enhancements_doc/Enhancement-6.md) |
| Case (in)equality `===` / `!==` | Lex and evaluate as `==`/`!=` — exact in a 2-state analog world (no x/z bits to distinguish). | — |
| Ternary `?:` | Including over string operands. | [E-37](../../enhancements_doc/Enhancement-37.md) |
| Based integer literals | `'h1F`, `'b101`, `'o17`, `'d42`, with underscores and explicit widths. `examples/escid_examples/` | [E-46](../../enhancements_doc/Enhancement-46.md) |
| Escaped identifiers | `\my-net!` per the LRM, everywhere identifiers appear. | [E-46](../../enhancements_doc/Enhancement-46.md) |
| String literal escapes | The full set (`\n`, `\t`, `\\`, `\"`, octal `\ddd`) via a single-pass unescaper. `examples/stresc_examples/` Since E-650 an octal escape above `\377` is refused (one 8-bit character, LRM 2.7.1) and an escape the LRM does not define (`\q`, `\x41`) is kept verbatim with a warning (L032). | [E-48](../../enhancements_doc/Enhancement-48.md), [E-650](../../enhancements_doc/Enhancement-650.md) |
| Real literal shape | LRM 2.6.2 wants a digit on each side of the decimal point: `1.` and, since E-650, `.5` (which was "unexpected token '.'") get the same sentence; `0.5`, `1.5e-3`, `2.5u` and dotted names are untouched. | [E-650](../../enhancements_doc/Enhancement-650.md) |
| Integer `min`/`max`/`abs`, away-from-zero rounding | Verified integer-typed, per the LRM. | [E-59](../../enhancements_doc/Enhancement-59.md) |
| Math functions & scale factors | `ln/log/exp/sqrt/pow/trig/hyperbolic/floor/ceil`, SI suffixes (`1k`, `1u`, …). | — |
| Expression depth | An expression may be 32 768 levels deep: an operator chain counts one level per operator (the passes after the parser recurse over the left-leaning tree it builds), and a parenthesis, a call, a prefix operator or a `?:` one nesting level each. Past that, one located error names the bound and what it counts, and stands alone. The bound was 1000 on the 8 MB main-thread stack, and a flat sum of a thousand terms tripped it; the front end runs on a 512 MB thread since E-718. `examples/robustness_examples/` | [E-718](../../enhancements_doc/Enhancement-718.md) |
## 2.6 Analog operators (filters, integrators, delays)

All analog operators produce correct **Jacobian contributions** via automatic
differentiation, so convergence behaves like a hand-written stamp.

| Operator | Notes | Since |
|---|---|---|
| `ddt(x)` | Time derivative; fully working (the reactive residual/Jacobian path). | — |
| `idt(x[, ic])` | Time integral; the initial condition survives into transient (it used to be zeroed at the IC phase). `examples/idtic_examples/` | [E-28](../../enhancements_doc/Enhancement-28.md) |
| `idt(x, ic, assert[, tol])` | Reset form: while `assert` is nonzero the state holds at `ic`; smooth reset dynamics keep self-referential resets bounded. Relaxation oscillators work. Since [E-678](../../enhancements_doc/Enhancement-678.md) the reset's time constant follows the transient's print step (a thousandth of it) instead of a fixed 10 µs, deadbeat at the onset, so a microsecond-scale hold reads `ic` throughout and resumes from it. `examples/idtassert_examples/` | [E-52](../../enhancements_doc/Enhancement-52.md) |
| `idtmod(x[, ic, modulus, offset])` | Modulo integrator (VCO phase idiom): integrates unbounded internally, wraps the returned value. A no-ic `idtmod` defaults its ic to 0 and pins the DC solution (LRM 4.5.5) instead of demanding feedback. `examples/idtmod_examples/` | [E-27](../../enhancements_doc/Enhancement-27.md) |
| `ddx(x, probe)` | Symbolic derivative w.r.t. a potential *or flow* probe — including the unnamed-branch flow `ddx(f, I(a,b))` (reversed orientation negates; a flow that is not a system unknown differentiates to 0). `examples/ddx_examples/` | [E-13](../../enhancements_doc/Enhancement-13.md), [E-59](../../enhancements_doc/Enhancement-59.md) |
| `absdelay(x, td[, maxdelay])` | Transport delay via the synthetic-node DAE approach. Without `maxdelay`, `td` is frozen at the first transient evaluation per LRM 4.5.7 (latched simulator-side at the converged operating point); with `maxdelay` it tracks and clamps. A transient whose operating point came through ngspice's Transient-op fallback keeps the delay ([E-671](../../enhancements_doc/Enhancement-671.md)). `examples/absdelay_examples/`, `examples/tranopdelay_examples/` | [E-1](../../enhancements_doc/Enhancement-1.md) |
| `transition(x[, td, rise, fall])` | 3/4/5-argument forms; `` `default_transition `` supplies defaults — for omitted **and explicit-zero** rise/fall times (LRM 4.5.8's "specified or are equal to zero"); without the directive, a bare `transition(x)` takes the LRM's negligible non-zero ramp. DC is well-posed. Since [E-698](../../enhancements_doc/Enhancement-698.md) the operator is realised by the simulator, like `absdelay`: ngspice schedules LRM 4.5.8's piecewise-linear ramp from each change of the *accepted* input — from the current output to the new value over `rise`/`fall` whatever the swing, corners as breakpoints, the interrupted-transition rule of Figures 4-7 to 4-12, a queue of pending transitions behind `td` — and stamps it as a function of time, so there is no tracking loop, no fixed `1/rise` rate (a 5 V step used to take `5·rise`) and no tail to ring under the trapezoidal rule (the plateau used to settle 2e-4 off, and `ddt` of it read a spurious current). AC is unity with the delay's phase. Since [E-696](../../enhancements_doc/Enhancement-696.md) a negative delay, rise or fall time the deck fixed is named at run time ("0 is used", LRM 4.5.8), where it was clamped in silence; a zero time without a directive is the LRM's negligible non-zero ramp (a thousandth of TSTEP), and a rise time under a picosecond is the same, where [E-697](../../enhancements_doc/Enhancement-697.md) once had to route it past a loop that aborted. Since [E-720](../../enhancements_doc/Enhancement-720.md) a source's edge fed straight in — a PULSE's 1 ns rise, which the integrator resolves with a dozen accepted points — is one change: every point after the edge's first readjusts against the ramp the first found, so an interrupted ramp runs at the LRM's (v3 − v2)/tf whatever the edge's width or the delay (it ran at 0.68/tf, set by where the timepoints fell), and the edge's ramp gets its corner breakpoints once the input holds still. `examples/transition_examples/`, `examples/defaulttransition_examples/`, `examples/transedge_examples/` | [E-6](../../enhancements_doc/Enhancement-6.md), [E-47](../../enhancements_doc/Enhancement-47.md), [E-696](../../enhancements_doc/Enhancement-696.md), [E-697](../../enhancements_doc/Enhancement-697.md), [E-698](../../enhancements_doc/Enhancement-698.md), [E-720](../../enhancements_doc/Enhancement-720.md) |
| `slew(x[, max_pos, max_neg])` | Rate limiter; honors the LRM's *negative* `max_neg_slew_rate` convention (and tolerates the legacy positive-magnitude spelling). Since [E-698](../../enhancements_doc/Enhancement-698.md) the limiter is the simulator's: `V(z) = clamp(V(y), y_last − neg·h, y_last + pos·h)` on the output accepted at the previous point — exactly the input while it moves within the bounds, an exact linear ramp at the bound otherwise, no tracking loop and no tail (LRM 4.5.9). Since [E-696](../../enhancements_doc/Enhancement-696.md) a deck-fixed rate outside LRM 4.5.9's domain is named at run time: a wrong sign uses its magnitude and says so, a zero rate drops the limit in that direction (it used to freeze the output for the whole run in silence); a rate at or above 1e12 V/s is simply a rate the limiter never has to enforce ([E-697](../../enhancements_doc/Enhancement-697.md) once routed it past a loop that aborted). `examples/slew_examples/`, `examples/opargs_examples/`, `examples/domainwarn_examples/`, `examples/transedge_examples/` | [E-6](../../enhancements_doc/Enhancement-6.md), [E-61](../../enhancements_doc/Enhancement-61.md), [E-696](../../enhancements_doc/Enhancement-696.md), [E-697](../../enhancements_doc/Enhancement-697.md), [E-698](../../enhancements_doc/Enhancement-698.md) |
| `laplace_nd/np/zd/zp` | Continuous-time filters via exact state-space realization; complex pole/zero pairs per the LRM's (re, im) vector convention; parameter-dependent coefficients. A **solution-dependent** coefficient tracks (the LRM would freeze it at analysis start) and draws a warning. `examples/laplace_examples/`, `examples/complexpole_examples/` The highest-order denominator coefficient must be a finite non-zero number at run time — "must be a finite non-zero number, but is nan" (or inf) since [E-708](../../enhancements_doc/Enhancement-708.md); a NaN was called "zero" and an infinity ran the filter as a silent 0. A module of many filters compiles in linear time and memory since [E-712](../../enhancements_doc/Enhancement-712.md): the root expansion of the `zp`/`np`/`zd` forms decides the zero-root exception at compile time for constant roots instead of opening four branch diamonds per root and coefficient, and a module of more than 256 Jacobian entries has its OSDI descriptor helpers built at -O0 — 100 twenty-pole filters in 1.6 s and 0.55 GB (113 s and 48 GB before). | [E-4](../../enhancements_doc/Enhancement-4.md), [E-31](../../enhancements_doc/Enhancement-31.md), [E-708](../../enhancements_doc/Enhancement-708.md), [E-712](../../enhancements_doc/Enhancement-712.md) |
| `zi_nd/np/zd/zp` | Z-domain filters via bilinear transform onto the laplace machinery — a **continuous** approximation: no discrete-time sample-and-hold (a unity z-filter is a wire), and the 6-argument form's `tau`/`t0` are accepted but ignored. `examples/zi_examples/` | [E-6](../../enhancements_doc/Enhancement-6.md), [E-31](../../enhancements_doc/Enhancement-31.md) A root **at the origin** is LRM 4.5.12's factor `z` — a one-period delay for a pole, an advance for a zero — since [E-699](../../enhancements_doc/Enhancement-699.md); a root the deck can set is never one (write a literal or `localparam` zero, or the `zi_nd` coefficients). |
| `last_crossing(x, dir)` | Interpolated crossing times, backed by simulator-side waveform history (an additive OSDI extension). Before any crossing it returns −1, LRM 4.5.10's negative value, in every analysis: the final step of an `ac` and a `noise` analysis read 0 there until [E-677](../../enhancements_doc/Enhancement-677.md). `examples/last_crossing_examples/` | [E-6](../../enhancements_doc/Enhancement-6.md) |
| `limexp(x)` | **Stateless** limited exponential (tangent-continued above the overflow cutoff) — a documented decision; see [§4.4](04-limitations-and-gotchas.md#44-documented-design-decisions). | [E-13](../../enhancements_doc/Enhancement-13.md) |
| `$limit(x, "pnjlim", …)` / `$limit(x, fn, …)` | Genuinely engages SPICE-style iteration limiting (stiff diodes converge without gmin stepping), for the built-in and user-function forms. `examples/opargs_examples/` | [E-61](../../enhancements_doc/Enhancement-61.md) |
| `$bound_step(t)` | Genuinely bounds the transient step; several calls in one evaluation honor the **smallest** active bound (LRM 9.17.2 — the last call used to win). | [E-61](../../enhancements_doc/Enhancement-61.md) |
| `$discontinuity(n)` | Clamps the *next* step and makes the integrator bisect onto the event instead of extrapolating across it. `examples/discontinuity_examples/` | [E-24](../../enhancements_doc/Enhancement-24.md), [E-55](../../enhancements_doc/Enhancement-55.md) |
| Trailing tolerance arguments | The optional `abstol`/nature args of `ddt`/`idt`/`idtmod`/`laplace_*`/`zi_*` are accepted and validated (positive, a real nature; the `laplace_*` one is checked since [E-700](../../enhancements_doc/Enhancement-700.md) — it passed unexamined before) but do **not** influence convergence tolerances — the OSDI ABI has no per-**equation** tolerance channel, so ngspice's global `abstol`/`reltol` govern. (A **nature's** declared `abstol` is a different matter and *does* reach the convergence test, per LRM 3.6.1 — see E-539.) Event tolerances likewise (a nonzero `cross`/`above` tolerance warns). | [E-61](../../enhancements_doc/Enhancement-61.md) |

Analog operators are statically forbidden inside conditionals whose condition
depends on the solution, and inside loops (LRM 4.5.1) — the diagnostics name
the actual construct and suggest hoisting or `generate` unrolling
([E-70](../../enhancements_doc/Enhancement-70.md)).

## 2.7 Contributions, branches, and probes

| Feature | Notes | Since |
|---|---|---|
| `V(a,b) <+` / `I(a,b) <+` contributions | Including switch branches and accumulation across statements, loops, and multiple analog blocks. A switch branch contributes the two kinds on **mutually exclusive conditional paths**; contributing both with no condition between them keeps only the last one and is reported by the `discarded_contribution` lint. A contribution whose access names no nature of the branch's disciplines (`Pwr(p,n) <+ …` across `electrical`/`thermal2`) names the incompatible disciplines (LRM 3.11.1) rather than "expected nature access such as V(foo)" since E-650. | [E-400](../../enhancements_doc/Enhancement-400.md), [E-650](../../enhancements_doc/Enhancement-650.md) |
| Indirect branch assignment | `V(out): V(x) == 0;` — the LRM's ideal-opamp construct, one implicit equation per statement. The LRM placement rules are enforced: illegal under a non-constant `if`/`case`/loop and inside event controls (a guarded-off constraint is a singular matrix), and mixing an indirect assignment with a direct `<+` on the same branch is an error naming both statements (LRM 5.6.7/5.6.7.2). A general real expression on the equality's left side is accepted as a generalized-implicit-equation extension. `examples/indirect_assignment_examples/` | [E-2](../../enhancements_doc/Enhancement-2.md) |
| Port-flow probes `I(<p>)` | Reading the total flow through a port (previously returned 0). `examples/portflow_examples/` | [E-29](../../enhancements_doc/Enhancement-29.md) |
| Probe-only branches | Probing a branch that is never contributed to reads its actual flow (an ideal ammeter) instead of 0 + open circuit; makes flow-only signal-flow disciplines work. `examples/portflow_examples/`, `examples/signalflow_examples/` | [E-36](../../enhancements_doc/Enhancement-36.md) |
| Grounded branch declarations | `branch (a) br;` | [E-59](../../enhancements_doc/Enhancement-59.md) |

## 2.8 Events

| Feature | Notes | Since |
|---|---|---|
| `@(initial_step)` / `@(final_step)` | Genuinely gated (once at the start; once at the end of a *successful* analysis, seeing the converged solution — the classic "report a tracked peak" pattern works). An op fires both. The final step of an `ac` or `noise` analysis sees the bias point the analysis linearised around, since [E-677](../../enhancements_doc/Enhancement-677.md) (it saw the last frequency's small-signal solution: `V(a,b)` read the response). Every analysis ends with it since [E-683](../../enhancements_doc/Enhancement-683.md): `pz`, `tf`, `sens`, `disto` and `sp` fire it once at their bias point, and what the block assigns stays readable afterwards under `ac`/`noise` as under `op` (the E-412 snapshot applies only when no bias point exists). `examples/initial_step_examples/`, `examples/finalstep_examples/`. Since E-648 an event control under a **non-constant `if`/`case`** is refused (LRM 5.8: only a constant condition — a literal, a parameter, `analysis()`, `$temperature` — may guard one; `cross`/`above` keep 5.10.3.1's loop rule): put the event at the top level and test the condition inside its body. `examples/eventcond_examples/` | [E-7](../../enhancements_doc/Enhancement-7.md), [E-53](../../enhancements_doc/Enhancement-53.md), [E-648](../../enhancements_doc/Enhancement-648.md) |
| Analysis-phase lists | `@(initial_step("ac","tran"))` filters by analysis type — LRM Table 5-1 exact per analysis: the OP of an `.ac`/`.noise` job belongs to that analysis, not to `"dc"`. | [E-53](../../enhancements_doc/Enhancement-53.md) |
| `cross(expr[, dir, time_tol, expr_tol])` | Edge detection with persistent state across evaluations. Fires only in transient analyses and only after t > 0 (LRM 5.10.3.2); its state still tracks through DC so the first step compares against the operating point. Tolerances are accepted but **not honored** (detection is evaluation-granular; a nonzero tolerance draws a warning). Not allowed under runtime `if`/`case`/loops (enforced; genvar forms stay legal). `examples/cross_examples/` A module of hundreds of `cross` events compiles in linear time since [E-713](../../enhancements_doc/Enhancement-713.md): each event's conjunctions are selects, and an eval whose straight-line code is one giant block is emitted through LLVM's fast instruction selector — 500 events in 0.5 s (119 s before), 2 000 in 2.6 s (unfinished before). | [E-8](../../enhancements_doc/Enhancement-8.md), [E-61](../../enhancements_doc/Enhancement-61.md), [E-713](../../enhancements_doc/Enhancement-713.md) |
| `above(expr[, …])` | Like `cross` but also fires in DC — including the LRM-mandated initialization event when the expression is positive at the initial solve. | [E-8](../../enhancements_doc/Enhancement-8.md), [E-59](../../enhancements_doc/Enhancement-59.md) |
| `timer(start[, period, tol])` | Periodic firing verified at exact counts; placement exact via the bound-step channel. `examples/timer_examples/` | [E-8](../../enhancements_doc/Enhancement-8.md), [E-61](../../enhancements_doc/Enhancement-61.md) |
| Event OR lists | `@(cross(…) or timer(…) or initial_step)` — fires exactly when any member fires (LRM 5.10.3); a comma is interchangeable with `or` (5.10.1). `examples/lrmcorner_examples/` | [E-59](../../enhancements_doc/Enhancement-59.md) |
| Invalid events rejected | `@(absdelta(…))`, named events, and typos are targeted **errors** — they used to silently drop the event and run the body on every evaluation. Nested `@(…)` is an error too. | — |

## 2.9 Analog functions

| Feature | Notes | Since |
|---|---|---|
| `analog function` basics | Inlined at the call site; derivatives flow through automatically. Since E-646 the function's **local variables start every call afresh** — a local read before the call's own write is its declared initializer (zero, or the empty string), a named block's local and the return array's elements included, on every call and every Newton iteration, as LRM 4.7.2 already made the identifier variable and the output arguments; state across calls belongs in a module variable. `examples/funclocal_examples/` | [E-646](../../enhancements_doc/Enhancement-646.md) |
| Array arguments | Whole-array `input` args ([E-18](../../enhancements_doc/Enhancement-18.md)), `output`/`inout` writeback ([E-20](../../enhancements_doc/Enhancement-20.md)), **array return values** (`real [0:2] f;` — [E-23](../../enhancements_doc/Enhancement-23.md)), array-literal arguments ([E-33](../../enhancements_doc/Enhancement-33.md)), array locals, and a **parameter array** as the argument of an `input` formal (1-D, N-D, integer; [E-645](../../enhancements_doc/Enhancement-645.md), which also names a size mismatch and refuses an `output`/`inout` formal fed a parameter array, since a parameter cannot receive the copy-back). `examples/paramarrayarg_examples/`, `examples/funcarray_examples/`, `examples/arrayout_examples/`, `examples/arrayret_examples/` | E-18/20/23/33 |
| Loops inside functions | Iterative algorithms (Newton, factorial) verified exact. | [E-70](../../enhancements_doc/Enhancement-70.md) |
| Integer/untyped arguments | Untyped args default to `real`; integer output args work. Since E-647 a real actual into an **integer input formal rounds** (LRM 4.7.3 with 4.2.1.1: `f(2.7)` is `f(3)`), as an assignment does; a real seed for `$random`/`$dist_*`, a real shift distance and an integer `output` formal fed a real variable stay refused. `examples/intformal_examples/` | [E-43](../../enhancements_doc/Enhancement-43.md), [E-59](../../enhancements_doc/Enhancement-59.md), [E-647](../../enhancements_doc/Enhancement-647.md) |
| String functions | `analog function string` return types and string `output` arguments (VAMS-2023, LRM 4.7.1). | [E-520](../../enhancements_doc/Enhancement-520.md) |
| Function-local `parameter`s | A `parameter` inside a function body is a compile-time local (LRM 4.7.1): it shadows a same-named module parameter, other module parameters read through — a netlist override of those propagates into the function — and it is never netlist-settable itself. | — |
| Output-array semantics | A pure `output` array is zero-initialized at entry, and an unassigned one resets the caller's array to zeros (LRM 4.7.2.3); `inout` arrays copy in and out. | — |
| Recursion | Not legal in Verilog-A; both direct and mutual recursion are clean, cycle-naming errors (mutual recursion used to crash the compiler). | [E-59](../../enhancements_doc/Enhancement-59.md) |

## 2.10 Procedural statements and loops

| Feature | Notes | Since |
|---|---|---|
| `if`/`else`, `case`, `casex`/`casez` | `case` works on strings and arrays; `casex`/`casez` treat `x`/`z`/`?` digits of item literals as comparison masks (the priority-encoder idiom), with don't-care literals rejected everywhere else. `examples/casexz_examples/` Since E-647 a `case` whose selector is an integer (or a boolean) and whose items include a real is compared as real (LRM 5.8.3 with 4.2.1): `case (sel) 2.0:` matches 2, `2.5:` matches nothing, where the item used to be a type error while the reverse converted. `examples/intformal_examples/` | [E-33](../../enhancements_doc/Enhancement-33.md), [E-59](../../enhancements_doc/Enhancement-59.md), [E-78](../../enhancements_doc/Enhancement-78.md), [E-647](../../enhancements_doc/Enhancement-647.md) |
| `for`, `while`, `repeat(n)`, `do…while` | All four, audited: nesting, loops over arrays, solution-dependent conditions, contributions accumulating inside loops. Parameter-dependent trip counts honor **model-card overrides at simulation time**. Note two deliberate relaxations beyond the LRM: contributions inside runtime loops (LRM 5.9 allows them only in the genvar `analog_for`) and `do…while` itself (absent from Annex A.6.8) — models relying on either are not portable LRM Verilog-A. `examples/analogloop_examples/`, `examples/dowhile_examples/`, `examples/repeat_examples/` `do…while` is not a Verilog-AMS loop (LRM 5.9 has the other three): it is an openvaf extension, said once under L011 since [E-661](../../enhancements_doc/Enhancement-661.md); `-A non_standard_code` silences it. | [E-9](../../enhancements_doc/Enhancement-9.md), [E-19](../../enhancements_doc/Enhancement-19.md), [E-70](../../enhancements_doc/Enhancement-70.md) |
| `disable <block>;` | Named-block early exit. Verilog-AMS allows it inside an analog event block only (A.6.4: `disable_statement` under `analog_event_statement`); in the plain analog block or an analog function it is an openvaf extension, said under L011 since [E-661](../../enhancements_doc/Enhancement-661.md) — a block ended by `disable` there skips the contributions after it in silence, and the standard loop exits are `break`/`continue` (next row). A named `begin : b` block inside an analog function compiles since E-661 (it crashed). `examples/disable_examples/` | [E-9](../../enhancements_doc/Enhancement-9.md), [E-661](../../enhancements_doc/Enhancement-661.md) |
| `break` / `continue` / `return` | The VAMS-2023 jump statements (LRM 5.11): `break`/`continue` in any runtime loop (`continue` re-enters a `for` at its increment), `return [expr]` exits an analog function early. Contextual keywords — legacy identifiers named `break` etc. still compile, with the L012 lint. Not allowed in genvar for-loops (LRM 5.9.3), and enforced. | — |

## 2.11 System tasks and functions

### Display and I/O

| Feature | Notes | Since |
|---|---|---|
| `$strobe`, `$display`, `$write`, `$monitor`, `$debug` | All five kinds; **full format-specifier surface** — `[flags][width][.precision]` on every conversion (`%5d`, `%-8s`, `%08.3f`, dynamic `%*d`), `%e/f/g/r` (engineering notation), `%d/h/o/b/c`, `%s`, `%m` (the **instance**'s hierarchical path, per LRM 9.4.4 — it named the *module* until E-539, so every instance printed one string; inside an inlined child instance it is the child's own path, `na1.l1.l2`, since E-650 -- every child printed the top instance's name before), `%%`. A task whose arguments are solution-independent runs in the model's setup code (the compiler hoists it, as every Verilog-A compiler does — the CMC models' parameter checks rely on it) and prints once per analysis, after the draws and the corner are applied, and again whenever a setup re-runs on its own: a `.dc temp` point, an `altermod` ([E-660](../../enhancements_doc/Enhancement-660.md)). `examples/display_examples/` A literal width or precision above 4096 is a compile error and a `*` width or precision is clamped to ±4096 at run time since [E-708](../../enhancements_doc/Enhancement-708.md): `%999999999d` was a 1 GB string, and past 2³¹ the C library dropped the specifier in silence. | [E-71](../../enhancements_doc/Enhancement-71.md), [E-539](../../enhancements_doc/Enhancement-539.md), [E-708](../../enhancements_doc/Enhancement-708.md) |
| File I/O | `$fopen`/`$fclose`/`$fdisplay`/`$fwrite`/`$fstrobe`/`$fmonitor`/`$fdebug`/`$fflush`/`$ftell`/`$fseek`/`$rewind`/`$feof`/`$ferror`. `examples/fileio_examples/` | [E-11](../../enhancements_doc/Enhancement-11.md) |
| Descriptors: the two kinds | `$fopen(name, mode)` returns a **file descriptor**; `$fopen(name)` returns a **multichannel descriptor** — a one-hot bit, with bit 0 reserved for stdout — and OR-ing several writes to all of them at once (`$fdisplay(mA\|mB, ...)`). The two are separate namespaces (LRM 9.5.1); they shared one until E-539, so an OR-ed write landed in an unrelated third file. `examples/lrmio_examples/` | [E-539](../../enhancements_doc/Enhancement-539.md) |
| Reading: what each call consumes | `$fgets` consumes the whole line; `$fscanf` consumes only what its format matches, so a following `$fgets` sees the **remainder** of that line. `$fclose` then `$fopen` for reading restarts at byte 0. `examples/lrmio_examples/` | [E-539](../../enhancements_doc/Enhancement-539.md) |
| String formatting/parsing | `$swrite`, `$sformat`, `$sscanf`, `$fgets`, `$fscanf`. `examples/stringio_examples/` | [E-11](../../enhancements_doc/Enhancement-11.md) |

### Modeling functions

| Feature | Notes | Since |
|---|---|---|
| `$table_model` | The LRM 9.21 surface: piecewise-linear (`1`), **cubic-spline** (`3`; with 9.21.4's end conditions since [E-704](../../enhancements_doc/Enhancement-704.md) — natural where an end extrapolates linearly, a zero end derivative where it holds the endpoint value under `C`, so the derivative is continuous into the constant extension; every spline was the natural one before) and **closest-point** (`D`, with the 9.21.4 tie rule) interpolation; **default linear extrapolation** per Tables 9-31/9-32 with per-dimension comma-separated control sub-strings, per-end `C`/`L`/`E` (error-on-extrapolation) characters — the `E` error judged on the accepted solution since [E-703](../../enhancements_doc/Enhancement-703.md), not on the Newton iterates on the way to it — and the `;N` dependent-column selector; data from the normative **N+M-column isoline files** (ragged isolines included), the self-describing N-D grid file (extension), interleaved `'{x0,y0,…}` or paired `'{xs}, '{ys}` arrays, and runtime array variables. Lowered to differentiable MIR, so *all* partial derivatives feed the Jacobian (a table-based MOSFET gets exact gm and gds). Since E-562: the **quadratic spline** (`2`); **`I` ignores a data column** of a file or of the array form; the **9.21.1 array data source** for a table of any dimension — column arrays (filled in `analog initial`, the textbook idiom, or initialised at declaration) or one 2-D array whose rows are the columns — read at compile time like a file, so the arrays must be compile-time constants (a run-time write, or an overridable `parameter` array, is refused with the reason); a **`localparam string`** as the file name or the control string (`file_name ::= string_literal \| string_parameter`; an overridable `parameter string` is refused — the table is built before the model card is read). Still refused: `I` on the runtime 1-D array form and on inline `'{...}` data. Since [E-702](../../enhancements_doc/Enhancement-702.md) the runtime 1-D array form **captures its data at the instance's first evaluation** of each analysis (LRM 9.21.1: "captured on the first call … any change after this point is ignored"): a knot computed from the solution makes one table for the whole analysis and draws the `table_data_captured` warning (L038), while a table built from parameters, constants, the temperature or an `@(initial_step)` fill is left live and keeps its compile-time folding (its data cannot change between two setups, where ngspice sets the instance up again: every analysis start, sweep point and `alter`). Since [E-705](../../enhancements_doc/Enhancement-705.md) a data file of any size compiles — the interval search is a binary tree (branches above 64 knots, branchless selects below) that picks the segment's parameters and evaluates one polynomial, so a 100 000-row file compiles in seconds where it overflowed the compiler's stack, and an evaluation costs `log2 n` branches instead of `n` polynomials. The result is an ordinary real: it can be assigned to a variable (an operating-point variable if attributed) and called inside an analog function, from literal arrays, a data file, arrays passed in as function arguments or arrays filled in the function body, with the slope intact in every case. `examples/table_model_examples/`, `examples/mdtable_examples/`, `examples/ndtable_examples/`, `examples/cubic_table_examples/`, `examples/tablesrc_examples/`, `examples/tablearray_examples/` Since [E-710](../../enhancements_doc/Enhancement-710.md) the spline's moments are solved by the Thomas algorithm in O(n) — in f64 for a data file, a literal or a `localparam` array, as straight-line MIR for an N-D slice — instead of through a dense n × n operator that took 77 s at 2 000 knots, and a file with a table above 4 096 knots is optimised without LLVM's SLP vectoriser, whose gather CSE is quadratic; 10 000 cubic knots compile in 1.5 s, 200 000 in 41 s. | [E-16](../../enhancements_doc/Enhancement-16.md), [E-17](../../enhancements_doc/Enhancement-17.md), [E-22](../../enhancements_doc/Enhancement-22.md), [E-40](../../enhancements_doc/Enhancement-40.md), [E-562](../../enhancements_doc/Enhancement-562.md), [E-702](../../enhancements_doc/Enhancement-702.md), [E-703](../../enhancements_doc/Enhancement-703.md), [E-704](../../enhancements_doc/Enhancement-704.md), [E-705](../../enhancements_doc/Enhancement-705.md), [E-710](../../enhancements_doc/Enhancement-710.md) |
| Noise sources | `white_noise`, `flicker_noise`, `noise_table`, `noise_table_log` (inline data or file; parameter-fed tables are honestly refused — the table bakes at compile time). **Correlation follows the call** (LRM 4.6.4.6): one call's output used in several contributions sums coherently (anti-phase uses cancel exactly), separate calls stay uncorrelated even under a shared label, and the label combines the contribution summary per 4.6.4.1; operating-point-dependent factors and `ddt()`-shaped noise (induced-gate idiom) are exact and add **no matrix unknowns**. `examples/noise_examples/`, `examples/noisecorr_examples/`, `examples/noisejw_examples/` A noise power the **model card** fixes to a negative number or a NaN (`white_noise(pw)` with `pw = -1e-18`) still makes the source inert (E-504's projection) and, since E-651, says so with a deferred `OSDI(warn)` line naming the builtin, the number and the substitute; a NaN `flicker_noise` exponent likewise; a zero power is a source switched off and passes without a word. | [E-9](../../enhancements_doc/Enhancement-9.md), [E-42](../../enhancements_doc/Enhancement-42.md), A `noise_table` of any length loads through a loop over three constant arrays since [E-713](../../enhancements_doc/Enhancement-713.md), with exactly the former select chain's answer for any data — 5 000 pairs compile in 0.5 s (58 s before), 100 000 in 2 s. [E-54](../../enhancements_doc/Enhancement-54.md), [E-651](../../enhancements_doc/Enhancement-651.md), [E-713](../../enhancements_doc/Enhancement-713.md) |
| `analysis("name", …)` | Variadic; matches ngspice's analysis phases per LRM Table 4-22, including the AC/noise operating point counting as `"ac"`/`"noise"` — and, since [E-684](../../enhancements_doc/Enhancement-684.md), the operating points of `.sp`, `.pz`, `.disto` and an AC `.sens` counting as `"ac"` too (`.tf` and a DC `.sens` are `"dc"`; `$simparam$str("analysis_name")` and the qualified step events follow the same rule), `"ic"`/`"static"` true at the transient's own operating point (the initial-condition idiom fires there, not at the first timestep), and `"nodeset"` true while `.nodeset` values are enforced; under `tran … uic`, where no operating point is solved, the ic-branch potential contributions seed the transient's start vector as a `.ic` on the nodes would ([E-689](../../enhancements_doc/Enhancement-689.md)). Since [E-722](../../enhancements_doc/Enhancement-722.md) the small-signal linearisation itself — the one evaluation whose Jacobians the whole `.ac`/`.noise` sweep uses — carries Table 4-22's AC or NOISE column (`"static"` 0, the sweep's name 1) rather than the operating point's, so an `idt` asserted on `analysis("static")`, LRM 4.5.5's own idiom, is an integrator in `.ac` and `.noise` (it was a short) and a topology switched on `"static"` linearises its dynamic branch; the operating point, the op variables and the events keep the AC-OP column. `examples/analysis_examples/`, `examples/idtassert_examples/` | [E-30](../../enhancements_doc/Enhancement-30.md), [E-53](../../enhancements_doc/Enhancement-53.md), [E-722](../../enhancements_doc/Enhancement-722.md) |
| `ac_stim([name, mag, phase])` | Full AC right-hand-side injection — a Verilog-A module can *be* the AC stimulus, with exact magnitude and phase (in **radians**). Activation matches the *running* small-signal analysis per 4.6.3: an `"ac"` stimulus stays out of `.noise` gain solves, and `ac_stim("noise")` participates there. `examples/acstim_examples/` | [E-26](../../enhancements_doc/Enhancement-26.md), [E-51](../../enhancements_doc/Enhancement-51.md) |
| Random / distributions | `$random`, `$arandom`, `$dist_*` and `$rdist_*` (uniform, normal, exponential, poisson, chi-square, t, erlang) — deterministic per `(seed, call site)`, stable across Newton iterations (reproducible Monte Carlo, no convergence breakage). Per the LRM, `$dist_*` returns `integer` and `$rdist_*` returns `real`. The 9.13.2 domain rules (mean/dof/k > 0) abort with the mandated runtime error on deck-supplied violations too, and `type_string` warns outside a paramset. The seed of `$arandom` and of the `$dist_*`/`$rdist_*` family is any integer value — a variable, a parameter, a literal (`$rdist_normal(7, 0, 1)`, Syntax 9-9's `[sign] decimal_number`) or an expression (`seed + 3`); since a draw is a function of the seed's value and the call site, `seed + i` inside a loop is a fresh draw per iteration, and lint L019 `rng_in_loop` stays quiet for a seed the loop changes. `$random` keeps Verilog's variable-only seed. `examples/rng_examples/`, `examples/distint_examples/`, `examples/seedexpr_examples/` A deck-fixed negative standard deviation or a reversed `$rdist_uniform`/`$dist_uniform` range is still projected (the mean, the start: E-505/506) and, since E-651, named through the same deferred warning; a run-time quantity is projected in silence as before. Since [E-709](../../enhancements_doc/Enhancement-709.md) a Poisson draw above a mean of 10 is Hörmann's transformed rejection (the multiplicative method returned a seed-dependent count near 750 for every mean above 745 (768 for the campaign's seed, 745 ± 28 over 2 000 seeds)) and the chi-square, t and Erlang draws above 256 degrees are a Marsaglia–Tsang gamma variate (the sums cost one draw per degree, and 2³¹ − 1 degrees never returned); below the thresholds every draw is what it was. | [E-10](../../enhancements_doc/Enhancement-10.md), [E-376](../../enhancements_doc/Enhancement-376.md), [E-642](../../enhancements_doc/Enhancement-642.md), [E-651](../../enhancements_doc/Enhancement-651.md), [E-709](../../enhancements_doc/Enhancement-709.md) |

### Environment and control

| Feature | Notes | Since |
|---|---|---|
| `$temperature`, `$vt[(T)]` | Simulator temperature (tracks `.dc temp` sweeps per point) and thermal voltage. | [E-62](../../enhancements_doc/Enhancement-62.md) |
| `$abstime`, `$realtime` | Identical in the continuous-time analog context. | [E-59](../../enhancements_doc/Enhancement-59.md) |
| `$simparam("name"[, default])`, `$simparam$str("name"[, default])` | Numeric and string simulator parameters; ngspice exposes `analysis_name` and `simulator` among others. The string form takes the LRM's optional default since E-598, so `instance`/`module`/`path`, which ngspice does not serve, can be asked for without a `$fatal`. Without a default an unserved name is the LRM's error: L025 at compile time, `$fatal` at the operating point, and since [E-676](../../enhancements_doc/Enhancement-676.md) the value the call hands on to the rest of that evaluation is the empty string (it was the U+FFFD replacement character). `examples/simparamstr_examples/`, `examples/simparamstrdef_examples/` | [E-25](../../enhancements_doc/Enhancement-25.md), [E-598](../../enhancements_doc/Enhancement-598.md) |
| `$finish`, `$stop`, `$fatal` | Honored at the accepted-point boundary: `$finish` ends the analysis cleanly (firing `@(final_step)`), `$stop` pauses resumably, `$fatal` aborts with its message — including under solution-dependent conditions, and during setup ("device rejected its configuration"). In a `.dc` sweep the `(at sweep value …)` label a run-time `$fatal` carries names the point being solved, and the sweep ends there with its own abort line, since [E-673](../../enhancements_doc/Enhancement-673.md) (it named the previous point, twice). `examples/simctrl_examples/` | [E-55](../../enhancements_doc/Enhancement-55.md), [E-56](../../enhancements_doc/Enhancement-56.md) |
| Fallback group | `$simprobe` (with a default; the no-default form is the LRM 9.16 error — warned at compile, fatal at run), `$analog_node_alias`, `$analog_port_alias` (analog-initial-only, enforced) compile and return their LRM "mechanism unavailable" fallbacks (see [§4.2](04-limitations-and-gotchas.md#42-features-with-fallback-semantics)). `examples/alias_examples/` | [E-12](../../enhancements_doc/Enhancement-12.md) |

## 2.12 Preprocessor and compiler directives

| Feature | Notes | Since |
|---|---|---|
| `` `define `` with arguments | Macros-using-macros, macro calls as macro arguments, backslash continuations, multi-line calls; **recursive expansion is a clean, located error** (both direct and mutual), while legitimate same-macro nesting inside arguments still works. `examples/preproc_examples/` `` `define G() `` (an empty formal list, 1364-2005 19.3.1) and a `\` continuation followed by white space get their own sentences since E-650. A reference to an undeclared macro is one error, at the reference, wherever it sits — in an expression ([E-665](../../enhancements_doc/Enhancement-665.md)), alone on its line at file scope (a misspelled directive) or in statement position ([E-672](../../enhancements_doc/Enhancement-672.md)); the preprocessor leaves a `0` in its place and the parser says nothing about it. | [E-65](../../enhancements_doc/Enhancement-65.md), [E-650](../../enhancements_doc/Enhancement-650.md) |
| `` `ifdef ``/`` `ifndef ``/`` `elsif ``/`` `else ``/`` `endif `` | Chained and nested, including inside module bodies; unbalanced conditionals are located errors. A stray `` `endif ``/`` `else `` is named as such since E-650 ("without a matching `` `ifdef ``"), not "encountered unexpected token!". | [E-65](../../enhancements_doc/Enhancement-65.md), [E-650](../../enhancements_doc/Enhancement-650.md) |
| `` `include `` | Nested chains; `-I` search paths. A file that includes itself, directly or through other files, is named at the include since E-650; it used to hit the 64-level depth limit and blame whichever include sat there. | [E-650](../../enhancements_doc/Enhancement-650.md) |
| `-D MACRO[=VALUE]` | Checked by shape on the command line since E-650: `-D G==2.0`, `` -D `G=2.0 `` and `-D 1G=2` are refused with the corrected spelling, and a value that fails to parse names its argument beside the virtual defines file. | [E-387](../../enhancements_doc/Enhancement-387.md), [E-460](../../enhancements_doc/Enhancement-460.md), [E-650](../../enhancements_doc/Enhancement-650.md) |
| Not text at all | A NUL byte in the source is reported as such (a binary, UTF-16 or truncated file?) instead of "encountered unexpected token!". | [E-650](../../enhancements_doc/Enhancement-650.md) |
| `` `default_transition `` | Supplies default rise/fall for bare `transition()` calls. `examples/defaulttransition_examples/` | [E-47](../../enhancements_doc/Enhancement-47.md) |
| Housekeeping directives | `` `celldefine ``/`` `endcelldefine ``, `` `unconnected_drive ``/`` `nounconnected_drive ``, `` `timescale ``, `` `line ``, `` `pragma ``, `` `undefineall ``, `` `resetall ``, `` `default_nettype `` — accepted (previously fatal errors). `examples/directive_examples/` `` `line `` moves `` `__FILE__ ``/`` `__LINE__ `` only and warns that diagnostics keep the physical position (L033, E-650). | [E-6](../../enhancements_doc/Enhancement-6.md), [E-650](../../enhancements_doc/Enhancement-650.md) |

## 2.12b Later language work

The sections above were written around the first wave of language support. The
following arrived afterwards and are all exercised by their own example suites.

**Declarations and ports.** Name-then-range declarations (`electrical in[0:2];`,
`input in[0:2];` — LRM 3.6/3.7) are normalised to the range-then-name form
([E-89](../../enhancements_doc/Enhancement-89.md)), multi-name declarations split per name
([E-91](../../enhancements_doc/Enhancement-91.md)), and a bus port's INPUT ordering bug was fixed
([E-90](../../enhancements_doc/Enhancement-90.md)). Parameters that shape a declaration width become
`localparam` so the width cannot change at simulation time
([E-92](../../enhancements_doc/Enhancement-92.md)), and a parameter that is never set is reported
([E-93](../../enhancements_doc/Enhancement-93.md)). Block-scoped parameters inside a named `begin`
are supported ([E-87](../../enhancements_doc/Enhancement-87.md)).

**Statements.** `casex`/`casez` with proper don't-care semantics — `z`/`?` for
`casez`, plus `x` for `casex` ([E-78](../../enhancements_doc/Enhancement-78.md)). Part-select on the
left and right of an assignment, and `` `__FILE__``/`` `__LINE__``
([E-85](../../enhancements_doc/Enhancement-85.md)). The legacy `generate` form with an `analog` block
inside ([E-88](../../enhancements_doc/Enhancement-88.md)) and the bare `generate` block
([E-96](../../enhancements_doc/Enhancement-96.md)).

**Builtins.** `$clog2` arity and its `ceil` crash ([E-101](../../enhancements_doc/Enhancement-101.md)–
[E-103](../../enhancements_doc/Enhancement-103.md)), `$rtoi`/`$itor` and the string functions
([E-104](../../enhancements_doc/Enhancement-104.md)–[E-108](../../enhancements_doc/Enhancement-108.md)), `$limit` for
genuine iteration limiting ([E-353](../../enhancements_doc/Enhancement-353.md)), and hierarchical
branch probes ([E-86](../../enhancements_doc/Enhancement-86.md)).

**The LRM's own examples as a suite.** [E-84](../../enhancements_doc/Enhancement-84.md) extracts every
code example from the LRM 2023 PDF and compiles it: 42 compile, 17 are documented
limitations pinned to their diagnostics, and 21 are correctly rejected as
mixed-signal (outside Annex C). The 146 non-module fragments double as a
no-crash corpus.

**Loops must be able to finish.** A provably non-terminating loop — `while (1)`,
or forgetting the increment — is a compile-time error
([E-375](../../enhancements_doc/Enhancement-375.md)):

```
error: loop condition is always true
       help: write what the condition reads inside the loop body, or in the `for` increment
```

A second message, `loop condition can never change`, covers a condition that is not
a literal but that nothing in the loop writes. The check is deliberately
conservative — `repeat (n)` is counted, `$finish`/`$stop`/`$fatal` leave the loop,
and a value passed to a function counts as written, since it may be an output
argument. What it cannot see is a loop whose variables change but never toward the
exit (nested loops sharing an index, where the bounds decide it).

## 2.13 Attributes

Attributes `(* … *)` are how a model talks to the simulator's UI:

| Attribute | Effect |
|---|---|
| `(* desc="…" *)` on a **variable** | Exposes it as an operating-point variable: `print @n1[var]`, `.save @n1[var]`, `.meas` over it — for real *and* integer variables ([E-69](../../enhancements_doc/Enhancement-69.md), `examples/opvar_examples/`). The name is looked up case-insensitively in the instance's table beside the instance parameters and ngspice's own `m`/`temp`; a twin or a `$` in it is reported at compile time since [E-652](../../enhancements_doc/Enhancement-652.md) |
| `(* type="instance" *)` on a **parameter** | Exposes it on the instance line and to `alter`/`.dc @n1[param]` ([E-62](../../enhancements_doc/Enhancement-62.md)) |
| `(* desc="…", units="…" *)` on a **parameter** | Description/units metadata in the OSDI descriptor |
| `(* std=σ *)` / `(* std_rel=f *)` / `(* dist="gauss"\|"uniform"\|"lognormal"\|"tgauss" *)` / `(* trunc=n *)` on a **parameter** | Declares the parameter's statistics for `.option osdimc` automatic Monte-Carlo (see [§3.6](03-ngspice-workflows.md#36-monte-carlo)); scalar real, non-local parameters only, and combining `std` with `std_rel` is an error. `dist="lognormal"` (alias `lnorm`) draws `nominal·exp(s·z)`, never crossing zero, with `std_rel` the sigma of the logarithm and an absolute `std` converted at the nominal; `trunc=n` confines the Gaussian coordinate to ±n sigmas (`dist="tgauss"` is gauss with `trunc=3`), and has no effect on a uniform ([E-554](../../enhancements_doc/Enhancement-554.md), `examples/osdidist_examples/`). A parameter the module tests with `$param_given` is drawn only when the deck gives it: a draw is a write, a write marks the parameter given, and a model that derives its default (`toxp = toxe − dtox` unless `toxp` is given) would switch branch instead of varying — the simulator says so once and leaves it alone ([E-555](../../enhancements_doc/Enhancement-555.md)). The sigma is a numeric literal, or a quoted number (`std="25.0"`) — anything else in the quotes (`"25 ohm"`, `"inf"`) is a located error (`examples/osdimc_examples/`). A sigma of 0 is a warning and is not exported; a `std_rel` on a parameter whose default is 0 is a warning too — with the default in force the sigma is 0 — and the simulator says so once per parameter when it meets the 0 at the draw ([E-620](../../enhancements_doc/Enhancement-620.md)). A statistical parameter whose default is derived from another parameter (`(* std *) parameter real r3 = 2*r`) draws around the default re-resolved from that trial's values, `2·r_drawn + δ` — the object exports which defaults are derived ([E-633](../../enhancements_doc/Enhancement-633.md)) |
| `(* corner="ss=115, ff=-10%, sf=+3sigma" *)` on a **parameter** | Declares the parameter's position at each named process corner for `.option corner=<name>` (see [§3.7](03-ngspice-workflows.md#37-statistical-modeling-inside-the-device)): `name=value` entries separated by commas and/or whitespace; a value is a real literal with an optional scale factor (the value at the corner), a percentage of the nominal, or a multiple of the declared `std`/`std_rel` through the transform a draw uses (a lognormal's log domain, a truncation's clamp, a uniform's half-width) — `sigma` needs the statistics. Names fold to lower case, as ngspice folds a deck. A malformed entry, a name twice, a non-string value, `sigma` without statistics, a corner named `tt`, `nom` or `nominal` (the nominal's spellings, which `.option corner=` reads as the nominal — [E-662](../../enhancements_doc/Enhancement-662.md)), and a name longer than 79 characters (ngspice selects a corner through its `corner` variable — [E-669](../../enhancements_doc/Enhancement-669.md)) are errors; a percentage of a zero default, a duplicate attribute, and the attribute on an integer, a string, a localparam or a variable are warnings. Exported through the `OSDI_CORNER_*` side table ([E-654](../../enhancements_doc/Enhancement-654.md)); a parameter the module tests with `$param_given` carries `OSDI_CORNER_GATED`, and the simulator moves it to the corner only when the deck gave it — the E-555 rule ([E-657](../../enhancements_doc/Enhancement-657.md)) |
| `(* openvaf_allow="…" *)`, `openvaf_warn`, `openvaf_deny` on a declaration, a module or a statement | Sets a lint's level for that scope (`--lints` lists them). An unknown lint name is refused (`lint_not_found`, L008 -- the report existed and was never emitted until [E-650](../../enhancements_doc/Enhancement-650.md)), with a "did you mean" for a near miss. |

A symmetry worth remembering: instance access to *parameters* needs
`type="instance"`; instance access to *variables* needs `desc`. And the
statistics attributes compose with it: a `(* type="instance", std=… *)`
parameter draws per instance (mismatch), a model-card one per card
(process).
